import React from 'react';
import styles from './styles.css';

const Header = (props) => {
    return (
        <header className="header">
            <div>Invybiz</div>
        </header>
    );
};

export default Header;